To make this work you need to name your files with gnuplot combatiple way.

Add the imports to the beginning of your file and do the plotting same way as in the example. 

It might need to be run twice for the plot to appear in the document.